# План решения проблем из анализа

**Feature**: Улучшение веб-версии SIFS Theory  
**Branch**: `002-sifs-web-enhancement`  
**Date**: 2025-01-27  
**Based on**: `analysis-report.md`

## Обзор

План решения **50 проблем** из анализа, разбитых на **4 этапа** по приоритетам:

- **Этап 1**: Критические проблемы (2 задачи) - немедленно
- **Этап 2**: Высокоприоритетные проблемы (12 задач) - перед MVP
- **Этап 3**: Средние проблемы (25 задач) - перед production
- **Этап 4**: Низкие проблемы (11 задач) - nice to have

**Всего задач**: 50 (можно выполнять параллельно в рамках этапа)

---

## Этап 1: Критические проблемы (CRITICAL)

**Цель**: Исправить критические проблемы, нарушающие конституцию или блокирующие базовую функциональность.

**Время**: 1-2 часа  
**Приоритет**: 🔴 КРИТИЧЕСКИЙ - выполнить немедленно

### Задача R1: Исправить проверку отсутствия sidebar/header в Layout

**Проблема**: A1 - В T018 нет явной проверки отсутствия sidebar/header (требование конституции)

**Действия**:

1. Открыть `specs/002-sifs-web-enhancement/tasks.md`
2. Найти задачу T018
3. Добавить явную проверку: "Layout.tsx БЕЗ sidebar/header, БЕЗ верхней навигации, только Orbital Dock"
4. Добавить комментарий: "Проверка соответствия конституции X. SSF-2025 Framework"

**Файлы для изменения**:

- `specs/002-sifs-web-enhancement/tasks.md` (T018)

**Критерии успеха**:

- ✅ T018 явно указывает отсутствие sidebar/header
- ✅ Упоминается соответствие конституции

---

### Задача R2: Добавить проверку скрытия /presentation из навигации

**Проблема**: A2 - В T020 нет явной проверки скрытия /presentation из OrbitalDock (FR-013)

**Действия**:

1. Открыть `specs/002-sifs-web-enhancement/tasks.md`
2. Найти задачу T020
3. Добавить явную проверку: "Проверить что /presentation НЕ отображается в OrbitalDock навигации, доступна только по прямой ссылке"
4. Добавить комментарий: "Требование FR-013: скрытие презентации из основной навигации"

**Файлы для изменения**:

- `specs/002-sifs-web-enhancement/tasks.md` (T020)

**Критерии успеха**:

- ✅ T020 явно указывает скрытие /presentation из навигации
- ✅ Упоминается FR-013

---

## Этап 2: Высокоприоритетные проблемы (HIGH)

**Цель**: Уточнить детали реализации и добавить недостающие проверки перед MVP.

**Время**: 4-6 часов  
**Приоритет**: 🟠 ВЫСОКИЙ - выполнить перед MVP

### Задача R3: Уточнить детали копирования кода и формул (FR-023)

**Проблема**: B1 - FR-023 требует копирование, но нет деталей реализации

**Действия**:

1. Открыть `specs/002-sifs-web-enhancement/spec.md`
2. Найти FR-023
3. Добавить детали после требования:
   ```
   - Использовать Clipboard API (navigator.clipboard.writeText)
   - Toast уведомления через shadcn/ui Sonner компонент
   - Обработка ошибок: fallback на старый метод (document.execCommand)
   - Формат уведомления: "Скопировано в буфер обмена" (русский/английский)
   ```

**Файлы для изменения**:

- `specs/002-sifs-web-enhancement/spec.md` (FR-023)

**Критерии успеха**:

- ✅ FR-023 содержит детали реализации
- ✅ Указаны библиотеки и методы

---

### Задача R4: Уточнить детали визуальных эффектов при взаимодействии (FR-025)

**Проблема**: B2 - FR-025 требует визуальные эффекты, но нет конкретных критериев

**Действия**:

1. Открыть `specs/002-sifs-web-enhancement/spec.md`
2. Найти FR-025
3. Добавить детали после требования:
   ```
   - Hover эффекты: scale 1.05 для кнопок, underline для ссылок, shadow increase для карточек, scale 1.2 для иконок
   - Click эффекты: ripple effect для кнопок (Motion), scale animation 0.95→1, glow effect на active
   - Scroll эффекты: parallax для фонов (0.5x), reveal animations для контента (fade-in 300ms), fade-in для секций
   - Консистентность: все эффекты должны использовать единые значения из theme.css
   ```

**Файлы для изменения**:

- `specs/002-sifs-web-enhancement/spec.md` (FR-025)

**Критерии успеха**:

- ✅ FR-025 содержит конкретные критерии для каждого типа эффекта
- ✅ Указаны значения и библиотеки

---

### Задача R5: Уточнить детали пошаговых выводов (FR-033)

**Проблема**: B3 - FR-033 требует пошаговые выводы, но нет деталей формата

**Действия**:

1. Открыть `specs/002-sifs-web-enhancement/spec.md`
2. Найти FR-033
3. Добавить детали после требования:
   ```
   - Структура шага: формула (LaTeX), объяснение (текст), результат (число с единицами)
   - Навигация: кнопки prev/next, прогресс-индикатор (шаг X из Y), возможность перехода к конкретному шагу
   - Анимация: fade-in 300ms для появления шага, плавная прокрутка к следующему шагу
   - Сохранение прогресса: localStorage ключ "sifs-derivation-progress-{calculationId}"
   ```

**Файлы для изменения**:

- `specs/002-sifs-web-enhancement/spec.md` (FR-033)

**Критерии успеха**:

- ✅ FR-033 содержит структуру шага и навигацию
- ✅ Указаны форматы и методы сохранения

---

### Задача R6: Уточнить детали истории расчётов (FR-034)

**Проблема**: B4 - FR-034 требует историю расчётов, но нет деталей

**Действия**:

1. Открыть `specs/002-sifs-web-enhancement/spec.md`
2. Найти FR-034
3. Добавить детали после требования:
   ```
   - Максимум 10 последних расчётов на направление теории
   - Формат сравнения: side-by-side таблица с параметрами и результатами
   - Экспорт: JSON (полные данные), CSV (только параметры и результаты)
   - Очистка: кнопка "Очистить историю" с подтверждением
   - Хранение: localStorage ключ "sifs-calculation-history-{direction}"
   ```

**Файлы для изменения**:

- `specs/002-sifs-web-enhancement/spec.md` (FR-034)

**Критерии успеха**:

- ✅ FR-034 содержит лимиты и форматы
- ✅ Указаны методы хранения и экспорта

---

### Задача R7: Уточнить детали модулей Markdown рендерера (FR-050.1)

**Проблема**: B5 - FR-050.1 детализирует модули, но не все детали учтены

**Действия**:

1. Открыть `specs/002-sifs-web-enhancement/spec.md`
2. Найти FR-050.1
3. Добавить детали для каждого модуля:
   ```
   - MarkdownRenderer.components.tsx:
     * Иконки для списков: CheckCircle (Lucide) для ordered, Circle (Lucide) для unordered
     * Таблицы: neomorphism класс neo-card, чередование строк (zebra striping)
   - MarkdownRenderer.images.tsx:
     * Подписи: italic стиль под изображением, формат "Рисунок X: описание"
     * Zoom: modal с full-size изображением, закрытие по ESC или клику вне
   ```

**Файлы для изменения**:

- `specs/002-sifs-web-enhancement/spec.md` (FR-050.1)

**Критерии успеха**:

- ✅ FR-050.1 содержит детали для всех модулей
- ✅ Указаны конкретные иконки и форматы

---

### Задача R8: Добавить проверку деталей Orbital Dock в задачу T021

**Проблема**: B6 - FR-069 детализирует Orbital Dock, но некоторые детали не проверяются

**Действия**:

1. Открыть `specs/002-sifs-web-enhancement/tasks.md`
2. Найти задачу T021
3. Добавить проверки:
   ```
   - backdrop-filter: blur(20px) opacity(0.1) (сверхпрозрачное стекло)
   - float frequency = --sifs-oscillation-speed (частота "дыхания" связана с параметром теории)
   - Проверить что все детали из FR-069 реализованы
   ```

**Файлы для изменения**:

- `specs/002-sifs-web-enhancement/tasks.md` (T021)

**Критерии успеха**:

- ✅ T021 содержит проверки всех деталей из FR-069
- ✅ Указаны конкретные значения CSS

---

### Задача R9: Добавить проверку деталей Fractal Dropdown в задачу T022

**Проблема**: B7 - FR-070 детализирует Fractal Dropdown, но некоторые детали не проверяются

**Действия**:

1. Открыть `specs/002-sifs-web-enhancement/tasks.md`
2. Найти задачу T022
3. Добавить проверки:
   ```
   - translateZ(-20px) scale(0.95) (Z-отдаление других блоков)
   - clip-path timing 300ms (анимация раскрытия)
   - Проверить что все детали из FR-070 реализованы
   ```

**Файлы для изменения**:

- `specs/002-sifs-web-enhancement/tasks.md` (T022)

**Критерии успеха**:

- ✅ T022 содержит проверки всех деталей из FR-070
- ✅ Указаны конкретные значения transform и timing

---

### Задача R10: Добавить явные задачи на проверку всех Success Criteria

**Проблема**: B8 - 39 Success Criteria не имеют явных задач на проверку

**Действия**:

1. Открыть `specs/002-sifs-web-enhancement/tasks.md`
2. Найти раздел "Phase 13: Polish & Cross-Cutting Concerns"
3. Добавить новый подраздел "Success Criteria Verification" перед существующими задачами
4. Создать задачи для каждого SC (SC-002 до SC-040):
   ```
   - [ ] T219 [P] Verify SC-002: All documents render correctly (100% pages) - test all document pages, check formulas, code, images, graphs
   - [ ] T220 [P] Verify SC-003: Simulation results display within 5 seconds - measure time for all simulations
   - [ ] T221 [P] Verify SC-004: Visual effects react in real-time (< 100ms) - measure delay for all spatial components
   ... (продолжить для всех 39 SC)
   ```

**Файлы для изменения**:

- `specs/002-sifs-web-enhancement/tasks.md` (добавить 39 новых задач)

**Критерии успеха**:

- ✅ Созданы задачи для всех 39 Success Criteria
- ✅ Каждая задача содержит конкретные критерии проверки

---

### Задача R11: Детализировать задачи тестирования (FR-059)

**Проблема**: B9 - FR-059 требует тестирование, но задачи поверхностные

**Действия**:

1. Открыть `specs/002-sifs-web-enhancement/tasks.md`
2. Найти задачи T180-T186
3. Детализировать каждую задачу:

   ```
   T180: Test all routes (FR-059, SC-032)
   - Создать чеклист маршрутов: /, /docs/theory/*, /docs/calculations/*, /docs/predictions/*, /docs/data/*, /docs/defense/*, /docs/protocol/*, /simulations/*, /presentation
   - Проверить что все маршруты работают, нет 404 ошибок
   - Критерий успеха: 100% маршрутов работают корректно

   T181: Test navigation (FR-059, SC-032)
   - Тест OrbitalDock: все категории открываются, подменю работают, переходы корректны
   - Тест FractalDropdownMenu: открытие/закрытие, Z-отдаление, touch gestures
   - Тест мобильной навигации: упрощённая версия, Sheet для подменю
   - Критерий успеха: все элементы навигации работают на desktop и mobile

   ... (продолжить для всех задач тестирования)
   ```

**Файлы для изменения**:

- `specs/002-sifs-web-enhancement/tasks.md` (T180-T186)

**Критерии успеха**:

- ✅ Все задачи тестирования содержат чеклисты и критерии успеха
- ✅ Указаны конкретные маршруты и элементы для проверки

---

### Задача R12: Уточнить детали проверки размера файлов линтерами (FR-042.3)

**Проблема**: B10 - FR-042.3 требует проверку линтерами, но нет деталей

**Действия**:

1. Открыть `specs/002-sifs-web-enhancement/spec.md`
2. Найти FR-042.3
3. Добавить детали после требования:
   ```
   - Использовать ESLint с правилом max-lines: ["error", { "max": 300, "skipComments": true, "skipBlankLines": true }]
   - Pre-commit hook: запускать ESLint перед коммитом, блокировать коммит при нарушении
   - Отчёт: выводить список файлов с количеством строк, предлагать разбиение на модули
   - Исключения: Markdown файлы документации, генерируемые файлы, конфигурационные файлы
   ```

**Файлы для изменения**:

- `specs/002-sifs-web-enhancement/spec.md` (FR-042.3)

**Критерии успеха**:

- ✅ FR-042.3 содержит детали линтера и pre-commit hook
- ✅ Указаны исключения и формат отчёта

---

### Задача R13: Добавить задачу на проверку лицензий всех зависимостей (FR-062)

**Проблема**: B11 - FR-062 требует только бесплатные ресурсы, но нет проверки всех зависимостей

**Действия**:

1. Открыть `specs/002-sifs-web-enhancement/tasks.md`
2. Найти задачу T162
3. Детализировать задачу:
   ```
   - [ ] T162 [P] Check licenses of all dependencies (FR-062, SC-036), document licenses in LICENSE file
     * Использовать npm-license-checker или license-checker для проверки всех зависимостей
     * Проверить что все лицензии MIT/ISC/CC0/Apache-2.0 (разрешённые бесплатные)
     * Создать файл LICENSE-DEPENDENCIES.md с полным списком зависимостей и их лицензий
     * Блокировать установку зависимостей с неразрешёнными лицензиями (GPL, AGPL, проприетарные)
     * Критерий успеха: 100% зависимостей имеют разрешённые лицензии
   ```

**Файлы для изменения**:

- `specs/002-sifs-web-enhancement/tasks.md` (T162)

**Критерии успеха**:

- ✅ T162 содержит детали проверки лицензий
- ✅ Указаны инструменты и критерии успеха

---

### Задача R14: Синхронизировать версии библиотек (FR-060)

**Проблема**: B12 - FR-060 требует конкретные версии, но в plan.md указаны другие версии

**Действия**:

1. Проверить актуальные версии в `package.json`
2. Открыть `specs/002-sifs-web-enhancement/spec.md`
3. Проверить версии в FR-060: Tailwind CSS 4.1.12, Motion 12.23.24, Recharts 2.15.2, Vite 6.3.5
4. Открыть `specs/002-sifs-web-enhancement/plan.md`
5. Обновить раздел "Primary Dependencies" с правильными версиями
6. Если версии в package.json отличаются, обновить package.json или уточнить в spec.md актуальные версии

**Файлы для изменения**:

- `specs/002-sifs-web-enhancement/plan.md` (Primary Dependencies)
- `package.json` (если нужно обновить версии)
- `specs/002-sifs-web-enhancement/spec.md` (FR-060, если версии отличаются)

**Критерии успеха**:

- ✅ Версии в spec.md, plan.md и package.json синхронизированы
- ✅ Указаны актуальные версии всех библиотек

---

## Этап 3: Средние проблемы (MEDIUM)

**Цель**: Уточнить неоднозначные требования и добавить проверки покрытия перед production.

**Время**: 8-12 часов  
**Приоритет**: 🟡 СРЕДНИЙ - выполнить перед production

### Задача R15: Уточнить критерии визуальной реакции на параметры теории (FR-011)

**Проблема**: C1 - FR-011 требует "реагировать визуально", но нет конкретных критериев

**Действия**:

1. Открыть `specs/002-sifs-web-enhancement/spec.md`
2. Найти FR-011
3. Добавить детали после требования:
   ```
   - Вибрация при --sifs-metric-stability < 0.5 (низкая стабильность)
   - Свечение при --sifs-metric-stability > 0.8 (высокая стабильность)
   - Ускорение частиц при --sifs-oscillation-speed > 1.0
   - Изменение цвета при --sifs-time-dilation-delta (синий → красный)
   ```

**Файлы для изменения**:

- `specs/002-sifs-web-enhancement/spec.md` (FR-011)

---

### Задача R16: Уточнить критерии визуального опыта музея (FR-010)

**Проблема**: C2 - FR-010 требует "визуальный опыт музея", но нет конкретных критериев

**Действия**:

1. Открыть `specs/002-sifs-web-enhancement/spec.md`
2. Найти FR-010
3. Добавить детали после требования:
   ```
   - Scroll-triggered animations: 300ms fade-in для контента
   - Parallax эффекты: 0.5x для фонов, 0.3x для элементов
   - Storytelling sections: постепенное раскрытие информации при прокрутке
   - Интерактивные экспонаты: hover эффекты, click анимации, плавные переходы
   ```

**Файлы для изменения**:

- `specs/002-sifs-web-enhancement/spec.md` (FR-010)

---

### Задача R17: Уточнить детали индекса документов (FR-012)

**Проблема**: C3 - FR-012 требует "индекс документов", но нет деталей

**Действия**:

1. Открыть `specs/002-sifs-web-enhancement/spec.md`
2. Найти FR-012
3. Добавить детали после требования:
   ```
   - Формат отображения: карточки с preview (первые 200 символов), изображение (если есть), метаданные
   - Количество на странице: 12 карточек, пагинация для остальных
   - Сортировка: по категории (теория, расчёты, предсказания), по дате создания (новые сначала)
   - Фильтрация: по категории, по ключевым словам, по дате
   ```

**Файлы для изменения**:

- `specs/002-sifs-web-enhancement/spec.md` (FR-012)

---

### Задача R18: Уточнить детали валидации параметров (FR-019)

**Проблема**: C4 - FR-019 требует "валидацию параметров", но нет деталей

**Действия**:

1. Открыть `specs/002-sifs-web-enhancement/spec.md`
2. Найти FR-019
3. Добавить детали после требования:
   ```
   - Валидация массы: диапазон 0.1 - 1e6, единицы измерения (кг)
   - Валидация масштаба: диапазон 0.01 - 100, безразмерная величина
   - Валидация скорости времени: диапазон 0.1 - 10, безразмерная величина
   - Сообщения об ошибках: на русском языке, понятные для пользователя, с примерами корректных значений
   - Предотвращение некорректных расчётов: блокировка кнопки "Запустить" при невалидных параметрах
   ```

**Файлы для изменения**:

- `specs/002-sifs-web-enhancement/spec.md` (FR-019)

---

### Задача R19: Уточнить детали предустановленных сценариев (FR-020)

**Проблема**: C5 - FR-020 требует "предустановленные сценарии", но нет деталей

**Действия**:

1. Открыть `specs/002-sifs-web-enhancement/spec.md`
2. Найти FR-020
3. Добавить детали после требования:
   ```
   - "Планета": масса=1, масштаб=1, скорость времени=1, описание="Стандартная планета"
   - "Звезда": масса=100, масштаб=10, скорость времени=0.5, описание="Массивная звезда"
   - "Чёрная дыра": масса=1e6, масштаб=0.01, скорость времени=0.1, описание="Сверхмассивная чёрная дыра"
   - Формат: кнопки с иконками и названиями, при клике автоматически заполняют параметры
   ```

**Файлы для изменения**:

- `specs/002-sifs-web-enhancement/spec.md` (FR-020)

---

### Задача R20: Уточнить детали compute shaders для частиц (FR-028.1)

**Проблема**: C6 - FR-028.1 требует compute shaders, но нет деталей реализации

**Действия**:

1. Открыть `specs/002-sifs-web-enhancement/spec.md`
2. Найти FR-028.1
3. Добавить детали после требования:
   ```
   - Compute shader для 10k частиц времени в фоне
   - Структура: position (vec3), velocity (vec3), lifetime (float)
   - Производительность: 60 FPS на современных GPU (RTX 3060 и выше)
   - Fallback: Canvas 2D для браузеров без WebGPU, уменьшенное количество частиц (1k)
   ```

**Файлы для изменения**:

- `specs/002-sifs-web-enhancement/spec.md` (FR-028.1)

---

### Задача R21: Уточнить детали батчинга обновлений (FR-029.2)

**Проблема**: C7 - FR-029.2 требует requestAnimationFrame с батчингом, но нет деталей

**Действия**:

1. Открыть `specs/002-sifs-web-enhancement/spec.md`
2. Найти FR-029.2
3. Добавить детали после требования:
   ```
   - Батч до 10 обновлений CSS переменных за кадр (16ms)
   - Дебаунсинг: 16ms для предотвращения избыточных обновлений
   - Стратегия: собирать все обновления за кадр, применять одним батчем в requestAnimationFrame
   - Приоритет: обновления от активной симуляции имеют приоритет
   ```

**Файлы для изменения**:

- `specs/002-sifs-web-enhancement/spec.md` (FR-029.2)

---

### Задача R22: Уточнить детали валидации с EHT, LIGO данными (FR-032)

**Проблема**: C8 - FR-032 требует валидацию с EHT, LIGO, но нет деталей

**Действия**:

1. Открыть `specs/002-sifs-web-enhancement/spec.md`
2. Найти FR-032
3. Добавить детали после требования:
   ```
   - EHT данные: для чёрных дыр (M87*, Sgr A*), сравнение размеров горизонта событий
   - LIGO данные: для гравитационных волн, сравнение частот и амплитуд
   - Точность: ±5% для совпадения с экспериментальными данными
   - Формат сравнения: таблица с теоретическими и экспериментальными значениями, процент отклонения
   ```

**Файлы для изменения**:

- `specs/002-sifs-web-enhancement/spec.md` (FR-032)

---

### Задача R23: Уточнить детали статистики использования симуляций (FR-044)

**Проблема**: C9 - FR-044 требует статистику, но нет деталей

**Действия**:

1. Открыть `specs/002-sifs-web-enhancement/spec.md`
2. Найти FR-044
3. Добавить детали после требования:
   ```
   - Метрики: количество запусков, последнее использование (дата/время), избранное (boolean)
   - Хранение: localStorage ключ "sifs-simulation-stats-{simulationId}"
   - Формат отображения: карточки с иконкой статистики, число запусков, дата последнего использования
   - Быстрый доступ: кнопка "Последние использованные" в SimulationsIndexPage, максимум 5 симуляций
   ```

**Файлы для изменения**:

- `specs/002-sifs-web-enhancement/spec.md` (FR-044)

---

### Задача R24: Уточнить детали MCP DALL-E генерации (FR-045)

**Проблема**: C10 - FR-045 требует MCP DALL-E, но нет деталей

**Действия**:

1. Открыть `specs/002-sifs-web-enhancement/spec.md`
2. Найти FR-045
3. Добавить детали после требования:
   ```
   - Hero изображение: 1920x1080, тематика SIFS Theory, сохранение в public/images/generated/hero.png
   - Иконки разделов: 256x256, по одной для каждого раздела (теория, расчёты, предсказания, данные, симуляции)
   - Фоновые паттерны: 1920x1080, абстрактные фрактальные паттерны, сохранение в public/images/generated/backgrounds/
   - Генерация: при сборке проекта (npm run build), кэширование результатов
   ```

**Файлы для изменения**:

- `specs/002-sifs-web-enhancement/spec.md` (FR-045)

---

### Задача R25: Уточнить детали экспорта графиков (FR-048)

**Проблема**: C11 - FR-048 требует экспорт графиков, но нет деталей

**Действия**:

1. Открыть `specs/002-sifs-web-enhancement/spec.md`
2. Найти FR-048
3. Добавить детали после требования:
   ```
   - PNG: 1920x1080, качество 300 DPI, формат RGBA
   - SVG: векторный формат, сохранение всех элементов графика (линии, точки, подписи)
   - PDF: A4 размер, качество 300 DPI, использование jsPDF библиотеки
   - Кнопка экспорта: в правом верхнем углу графика, выпадающее меню с форматами
   ```

**Файлы для изменения**:

- `specs/002-sifs-web-enhancement/spec.md` (FR-048)

---

### Задача R26: Добавить задачу на автоматическую проверку "одна страница - одна цель" (FR-053)

**Проблема**: C12 - FR-053 требует правило, но нет проверки выполнения

**Действия**:

1. Открыть `specs/002-sifs-web-enhancement/tasks.md`
2. Найти задачу T116
3. Детализировать задачу:
   ```
   - [ ] T116 Verify all pages follow "one page - one goal" rule (FR-053, SC-027): maximum 2-3 types of visual effects per page
     * Создать скрипт scripts/check-page-goals.ps1 для автоматической проверки
     * Подсчитать типы визуальных эффектов на каждой странице (hover, click, scroll, parallax, animations)
     * Проверить что каждая страница имеет максимум 2-3 типа эффектов
     * Отчёт: список страниц с количеством типов эффектов, рекомендации по упрощению
     * Критерий успеха: 100% страниц следуют правилу
   ```

**Файлы для изменения**:

- `specs/002-sifs-web-enhancement/tasks.md` (T116)

---

### Задача R27: Добавить задачу на создание скрипта проверки консистентности (FR-054)

**Проблема**: C13 - FR-054 требует проверку консистентности, но нет автоматизации

**Действия**:

1. Открыть `specs/002-sifs-web-enhancement/tasks.md`
2. Найти задачу T117
3. Детализировать задачу:
   ```
   - [ ] T117 Verify design consistency (FR-054, SC-026): spacing, typography, colors on all pages
     * Создать скрипт scripts/check-design-consistency.ps1
     * Анализ CSS классов: проверка использования spacing (xs, sm, md, lg, xl, 2xl) из theme.css
     * Анализ типографики: проверка использования h1-h4, base, small из theme.css
     * Анализ цветов: проверка использования только цветов из theme.css (Level 0, Level 1, Level 2, Neomorphism)
     * Отчёт: список нарушений с указанием страниц и компонентов
     * Критерий успеха: 100% страниц используют только определённые значения из theme.css
   ```

**Файлы для изменения**:

- `specs/002-sifs-web-enhancement/tasks.md` (T117)

---

### Задача R28: Добавить задачу на валидацию типографики (FR-055)

**Проблема**: C14 - FR-055 требует типографику, но нет проверки применения

**Действия**:

1. Открыть `specs/002-sifs-web-enhancement/tasks.md`
2. Найти задачу T172
3. Детализировать задачу:
   ```
   - [ ] T172 [P] Verify line-height 1.6 for text and 1.2 for headings (FR-055, SC-028) on all pages
     * Создать скрипт scripts/check-typography.ps1
     * Проверка line-height: 1.6 для текста (p, span, li), 1.2 для заголовков (h1-h4)
     * Проверка max-width: 800px для текстовых блоков
     * Отчёт: список страниц с нарушениями, рекомендации по исправлению
     * Критерий успеха: 100% страниц используют правильные line-height и max-width
   ```

**Файлы для изменения**:

- `specs/002-sifs-web-enhancement/tasks.md` (T172)

---

### Задача R29: Добавить задачу на валидацию цветовой палитры (FR-056)

**Проблема**: C15 - FR-056 требует цветовую палитру, но нет проверки использования

**Действия**:

1. Открыть `specs/002-sifs-web-enhancement/tasks.md`
2. Найти задачу T175
3. Детализировать задачу:
   ```
   - [ ] T175 [P] Verify only defined colors from theme.css are used (FR-056, SC-029) on all pages
     * Создать скрипт scripts/check-color-palette.ps1
     * Проверка использования только цветов из theme.css: Level 0 (oklch(0% 0 0)), Level 1 (rgba(15, 15, 15, 0.7)), Level 2 (rgba(255, 255, 255, 0.03)), Neomorphism база (#1e1e1e)
     * Поиск неопределённых цветов: анализ всех CSS файлов и компонентов на использование цветов вне палитры
     * Отчёт: список нарушений с указанием файлов и строк
     * Критерий успеха: 100% страниц используют только цвета из theme.css
   ```

**Файлы для изменения**:

- `specs/002-sifs-web-enhancement/tasks.md` (T175)

---

### Задача R30: Добавить задачу на валидацию shadcn/ui с neomorphism (FR-057)

**Проблема**: C16 - FR-057 требует shadcn/ui с neomorphism, но нет проверки применения

**Действия**:

1. Открыть `specs/002-sifs-web-enhancement/tasks.md`
2. Найти задачу T148
3. Детализировать задачу:
   ```
   - [ ] T148 [P] Verify all existing components use shadcn/ui with neomorphism effects consistently (FR-057, SC-030)
     * Создать скрипт scripts/check-shadcn-neomorphism.ps1
     * Проверка использования shadcn/ui компонентов: Card, Button, Tabs, Accordion, Sheet, Badge, Separator, Tooltip, Popover
     * Проверка применения neomorphism классов: neo-raised, neo-pressed, neo-card, neo-glow, neo-grid
     * Отчёт: список компонентов без neomorphism эффектов
     * Критерий успеха: 100% компонентов используют shadcn/ui с neo- классами
   ```

**Файлы для изменения**:

- `specs/002-sifs-web-enhancement/tasks.md` (T148)

---

### Задача R31: Добавить задачу на измерение производительности (FR-063)

**Проблема**: C17 - FR-063 требует оптимизацию, но нет измерения результатов

**Действия**:

1. Открыть `specs/002-sifs-web-enhancement/tasks.md`
2. Найти задачу T121
3. Добавить новую задачу после T121:
   ```
   - [ ] T222 [P] Measure performance before and after optimization (FR-063, SC-011, SC-012, SC-017)
     * Использовать browser DevTools Performance для измерения загрузки страниц
     * Метрики: время загрузки (target < 3 секунд), FPS для анимаций (target 60 FPS), FPS для WebGPU частиц (target 60 FPS)
     * Измерение до оптимизации: базовые метрики для всех страниц
     * Измерение после оптимизации: сравнение метрик, проверка улучшения
     * Отчёт: таблица с метриками до/после, процент улучшения
     * Критерий успеха: все метрики соответствуют Success Criteria
   ```

**Файлы для изменения**:

- `specs/002-sifs-web-enhancement/tasks.md` (добавить T222)

---

### Задача R32: Добавить задачу на тестирование всех размеров экранов (FR-064)

**Проблема**: C18 - FR-064 требует мобильную адаптацию, но нет проверки на всех размерах

**Действия**:

1. Открыть `specs/002-sifs-web-enhancement/tasks.md`
2. Найти задачу T066
3. Детализировать задачу:
   ```
   - [ ] T066 [US5] Verify all pages work correctly on all screen sizes (320px-1920px+) with functional elements (FR-064, SC-005, SC-037)
     * Создать чеклист размеров экранов: 320px, 640px, 768px, 1024px, 1280px, 1920px
     * Проверка на каждом размере: навигация работает, текст читаем, графики масштабируются, таблицы прокручиваются
     * Использовать browser DevTools Responsive Design Mode для тестирования
     * Отчёт: список страниц с проблемами на конкретных размерах экранов
     * Критерий успеха: 100% страниц работают корректно на всех размерах экранов
   ```

**Файлы для изменения**:

- `specs/002-sifs-web-enhancement/tasks.md` (T066)

---

### Задача R33: Добавить задачу на автоматическую проверку консистентности (FR-068)

**Проблема**: C19 - FR-068 требует консистентность, но нет автоматической проверки

**Действия**:

1. Открыть `specs/002-sifs-web-enhancement/tasks.md`
2. Найти задачу T111
3. Добавить новую задачу после T113:
   ```
   - [ ] T223 [P] Create automatic consistency check script (FR-068, SC-023, SC-026, SC-027, SC-028, SC-029)
     * Создать скрипт scripts/check-consistency.ps1 объединяющий проверки spacing, typography, colors
     * Интеграция с существующими скриптами: check-design-consistency.ps1, check-typography.ps1, check-color-palette.ps1
     * Запуск: npm run check-consistency (добавить в package.json scripts)
     * Отчёт: сводный отчёт со всеми нарушениями консистентности
     * Критерий успеха: скрипт работает и выявляет все нарушения
   ```

**Файлы для изменения**:

- `specs/002-sifs-web-enhancement/tasks.md` (добавить T223)

---

### Задача R34: Уточнить 4 уровня реальности в plan.md (FR-026)

**Проблема**: C20 - FR-026 требует 4 уровня, но в plan.md не все уровни явно указаны

**Действия**:

1. Открыть `specs/002-sifs-web-enhancement/plan.md`
2. Найти раздел "Project Structure"
3. Добавить комментарий в структуру:
   ```
   ### SSF-2025 Levels Mapping
   - Level 0 (Temporal Abyss): TemporalAbyss.tsx - фон с частицами времени
   - Level 1 (Substrate): Layout.tsx - подложка страницы с neomorphism эффектами
   - Level 2 (Control Plane): OrbitalDock.tsx - навигация с парящими островками
   - Level 3 (Pulse): DeltaPulse.tsx - индикаторы с динамическим цветом
   ```

**Файлы для изменения**:

- `specs/002-sifs-web-enhancement/plan.md` (Project Structure)

---

### Задача R35: Проверить покрытие всех Spatial компонентов (FR-027)

**Проблема**: C21 - FR-027 перечисляет 10 компонентов, но некоторые упоминаются поверхностно

**Действия**:

1. Открыть `specs/002-sifs-web-enhancement/tasks.md`
2. Проверить наличие задач для всех 10 компонентов:
   - SpatialSlab (T100)
   - OrbitalDock (T021)
   - DeltaPulse (T069)
   - FrequencyKnob (T070)
   - ChronoOdometer (T071)
   - TemporalWave (T072)
   - SyncOrb (T073)
   - BentoCell (T024)
   - SpatialScrollbar (T101)
   - FractalDropdownMenu (T022)
3. Если какой-то компонент упоминается поверхностно, детализировать задачу

**Файлы для изменения**:

- `specs/002-sifs-web-enhancement/tasks.md` (проверить и обновить задачи)

---

### Задача R36: Разделить T086 на отдельные задачи для каждого направления расчётов (FR-031)

**Проблема**: C22 - FR-031 требует 10 направлений, но в T086 некоторые объединены

**Действия**:

1. Открыть `specs/002-sifs-web-enhancement/tasks.md`
2. Найти задачу T086
3. Разделить на отдельные задачи:
   ```
   - [ ] T086.1 [US8] Create calculation component for fractal structure in InteractiveCalculationsPage
   - [ ] T086.2 [US8] Create calculation component for brane tension in InteractiveCalculationsPage
   - [ ] T086.3 [US8] Create calculation component for quantum entanglement in InteractiveCalculationsPage
   - [ ] T086.4 [US8] Create calculation component for optical metric in InteractiveCalculationsPage
   - [ ] T086.5 [US8] Create calculation component for RS2 geometry in InteractiveCalculationsPage
   - [ ] T086.6 [US8] Create calculation component for mirror zones in InteractiveCalculationsPage
   - [ ] T086.7 [US8] Create calculation component for electron as torus in InteractiveCalculationsPage
   ```

**Файлы для изменения**:

- `specs/002-sifs-web-enhancement/tasks.md` (T086)

---

### Задача R37: Разделить T091 на отдельные задачи для каждого зала (FR-035)

**Проблема**: C23 - FR-035 требует 6 залов, но в T091 все упоминаются вместе

**Действия**:

1. Открыть `specs/002-sifs-web-enhancement/tasks.md`
2. Найти задачу T091
3. Разделить на отдельные задачи или явно перечислить все 6 залов:
   ```
   - [ ] T091 [US9] Create WorldChangePage.tsx with epic entrance animation and 6 halls:
     * Зал 1: Будущее энергетики
     * Зал 2: Путешествия во времени
     * Зал 3: Квантовые вычисления
     * Зал 4: Космические путешествия
     * Зал 5: Медицина будущего
     * Зал 6: Связь и коммуникации
     * Ensure page load time < 5 seconds (SC-021)
   ```

**Файлы для изменения**:

- `specs/002-sifs-web-enhancement/tasks.md` (T091)

---

### Задача R38: Проверить покрытие всех музейных компонентов (FR-037)

**Проблема**: C24 - FR-037 перечисляет 13 компонентов, но некоторые упоминаются поверхностно

**Действия**:

1. Открыть `specs/002-sifs-web-enhancement/tasks.md`
2. Проверить наличие задач для всех 13 компонентов:
   - MuseumHall (T048)
   - InteractiveExhibit (T049)
   - StorytellingSection (T050)
   - WorldChangeExample (T092)
   - MuseumMap (T077)
   - VirtualGuide (T079)
   - TimelineExhibit (T094)
   - BeforeAfterComparison (T093)
   - ParallaxHero (T051)
   - ParticleCursor (T052)
   - ExhibitCard (T095)
   - HallCard (T096)
   - RouteSelector (T078)
3. Если какой-то компонент упоминается поверхностно, детализировать задачу

**Файлы для изменения**:

- `specs/002-sifs-web-enhancement/tasks.md` (проверить и обновить задачи)

---

### Задача R39: Объединить дублирование требований к spacing системе (FR-040, FR-068)

**Проблема**: C25 - FR-040 и FR-068 дублируют требования к spacing

**Действия**:

1. Открыть `specs/002-sifs-web-enhancement/spec.md`
2. Найти FR-040
3. Удалить дублирование, оставить ссылку на FR-068:
   ```
   - **FR-040**: Система ДОЛЖНА использовать единую spacing систему (см. FR-068 для деталей)
   ```
4. Убедиться что FR-068 содержит полные детали spacing системы

**Файлы для изменения**:

- `specs/002-sifs-web-enhancement/spec.md` (FR-040)

---

## Этап 4: Низкие проблемы (LOW)

**Цель**: Уточнить детали для улучшения качества спецификации (nice to have).

**Время**: 4-6 часов  
**Приоритет**: 🟢 НИЗКИЙ - выполнить по возможности

### Задача R40: Уточнить детали структуры i18n (FR-014)

**Проблема**: D1 - FR-014 требует "базовую структуру i18n", но нет деталей

**Действия**:

1. Открыть `specs/002-sifs-web-enhancement/spec.md`
2. Найти FR-014
3. Добавить детали после требования:
   ```
   - Только интерфейс переводится (не содержимое документов)
   - JSON формат для файлов локалей
   - Структура: common.json (общие строки), nav.json (навигация), pages.json (заголовки страниц)
   - Языки: русский (ru) и английский (en)
   ```

**Файлы для изменения**:

- `specs/002-sifs-web-enhancement/spec.md` (FR-014)

---

### Задача R41: Уточнить детали сохранения языка (FR-015)

**Проблема**: D2 - FR-015 требует сохранение языка, но нет деталей

**Действия**:

1. Открыть `specs/002-sifs-web-enhancement/spec.md`
2. Найти FR-015
3. Добавить детали после требования:
   ```
   - Ключ localStorage: "sifs-language"
   - Значения: "ru" (русский) или "en" (английский)
   - Fallback: "ru" при ошибке чтения или отсутствии значения
   - Обработка ошибок: try-catch при чтении/записи, логирование ошибок
   ```

**Файлы для изменения**:

- `specs/002-sifs-web-enhancement/spec.md` (FR-015)

---

### Задача R42: Уточнить критерий "длинный документ" для оглавления (FR-017)

**Проблема**: D3 - FR-017 требует оглавление, но нет критерия "длинный"

**Действия**:

1. Открыть `specs/002-sifs-web-enhancement/spec.md`
2. Найти FR-017
3. Добавить детали после требования:
   ```
   - Критерий "длинный документ": > 5000 слов ИЛИ > 10 заголовков (h2-h4)
   - Автоматическое определение: подсчёт слов и заголовков при загрузке документа
   - Формат оглавления: список заголовков с якорными ссылками, плавная прокрутка к разделу
   - Позиция: фиксированная справа на desktop, скрыта на mobile (или в выпадающем меню)
   ```

**Файлы для изменения**:

- `specs/002-sifs-web-enhancement/spec.md` (FR-017)

---

### Задача R43: Уточнить критерии связи документов (FR-018)

**Проблема**: D4 - FR-018 требует связанные документы, но нет критериев связи

**Действия**:

1. Открыть `specs/002-sifs-web-enhancement/spec.md`
2. Найти FR-018
3. Добавить детали после требования:
   ```
   - Критерии связи: по категории (теория, расчёты, предсказания), по ключевым словам в метаданных
   - Максимум 5 связанных документов на странице
   - Формат отображения: карточки с preview, ссылки на связанные документы
   - Позиция: внизу страницы документа, секция "Связанные документы"
   ```

**Файлы для изменения**:

- `specs/002-sifs-web-enhancement/spec.md` (FR-018)

---

### Задача R44: Уточнить детали fallback-решений (FR-021)

**Проблема**: D5 - FR-021 требует fallback, но нет деталей

**Действия**:

1. Открыть `specs/002-sifs-web-enhancement/spec.md`
2. Найти FR-021
3. Добавить детали после требования:
   ```
   - Проверка поддержки: navigator.gpu для WebGPU, fallback на Canvas 2D
   - Поддержка браузеров: Chrome 113+, Firefox 110+, Safari 16.4+, Edge 113+
   - IE11 не поддерживается (требуется современный браузер)
   - Fallback стратегия: автоматическое определение поддержки, переключение на Canvas при отсутствии WebGPU
   ```

**Файлы для изменения**:

- `specs/002-sifs-web-enhancement/spec.md` (FR-021)

---

### Задача R45: Уточнить детали индикаторов загрузки (FR-022)

**Проблема**: D6 - FR-022 требует индикаторы, но нет деталей

**Действия**:

1. Открыть `specs/002-sifs-web-enhancement/spec.md`
2. Найти FR-022
3. Добавить детали после требования:
   ```
   - Тип индикатора: spinner (крутящийся) для документов, progress bar (полоса прогресса) для симуляций
   - Когда показывать: при загрузке > 500ms (чтобы избежать мигания при быстрой загрузке)
   - Позиция: центр экрана для документов, над симуляцией для симуляций
   - Анимация: плавное появление/исчезновение (fade 200ms)
   ```

**Файлы для изменения**:

- `specs/002-sifs-web-enhancement/spec.md` (FR-022)

---

### Задача R46: Уточнить детали метаданных документов (FR-024)

**Проблема**: D7 - FR-024 требует метаданные, но нет деталей

**Действия**:

1. Открыть `specs/002-sifs-web-enhancement/spec.md`
2. Найти FR-024
3. Добавить детали после требования:
   ```
   - Метаданные: дата создания, категория, зал музея (опционально)
   - Формат отображения: "Создано: 2025-01-27 | Категория: Теория | Зал: Основы SIFS"
   - Позиция: вверху страницы документа, под заголовком
   - Стиль: мелкий текст, серый цвет, разделитель "|"
   ```

**Файлы для изменения**:

- `specs/002-sifs-web-enhancement/spec.md` (FR-024)

---

### Задача R47: Уточнить критерий "большой компонент" (FR-042.1)

**Проблема**: D8 - FR-042.1 требует разбиение, но нет критерия "большой"

**Действия**:

1. Открыть `specs/002-sifs-web-enhancement/spec.md`
2. Найти FR-042.1
3. Добавить детали после требования:
   ```
   - Критерий "большой компонент": > 300 строк ИЛИ > 10 пропсов
   - Разбиение на части: Header (верхняя часть), Body (основной контент), Footer (нижняя часть)
   - Для страниц: вынос секций в отдельные компоненты (Hero, Stats, Features, Visual Examples)
   ```

**Файлы для изменения**:

- `specs/002-sifs-web-enhancement/spec.md` (FR-042.1)

---

### Задача R48: Добавить задачу на валидацию структуры модулей (FR-042.2)

**Проблема**: D9 - FR-042.2 требует структуру модулей, но нет проверки

**Действия**:

1. Открыть `specs/002-sifs-web-enhancement/tasks.md`
2. Найти задачу T179
3. Детализировать задачу:
   ```
   - [ ] T179 [P] Validate module structure (FR-042.2, SC-034) for all new components
     * Создать скрипт scripts/check-module-structure.ps1
     * Проверка наличия .types.ts, .hooks.ts, .utils.ts, .config.ts для компонентов > 200 строк
     * Отчёт: список компонентов без правильной структуры модулей
     * Критерий успеха: 100% компонентов > 200 строк имеют правильную структуру модулей
   ```

**Файлы для изменения**:

- `specs/002-sifs-web-enhancement/tasks.md` (T179)

---

### Задача R49: Уточнить детали синхронизации визуальных эффектов (FR-049)

**Проблема**: D10 - FR-049 требует синхронизацию, но нет деталей

**Действия**:

1. Открыть `specs/002-sifs-web-enhancement/spec.md`
2. Найти FR-049
3. Добавить детали после требования:
   ```
   - Синхронизация через общий контекст: SpatialConnectorContext для всех активных симуляций
   - Приоритет: активная симуляция (в фокусе) имеет приоритет для обновления CSS переменных
   - Дебаунсинг: 100ms для предотвращения избыточных обновлений при быстрых изменениях
   - Стратегия: объединение параметров от всех активных симуляций, применение средневзвешенных значений
   ```

**Файлы для изменения**:

- `specs/002-sifs-web-enhancement/spec.md` (FR-049)

---

### Задача R50: Объединить дублирование требований к типографике (FR-041, FR-055, FR-068)

**Проблема**: D11 - FR-041, FR-055 и FR-068 дублируют требования к типографике

**Действия**:

1. Открыть `specs/002-sifs-web-enhancement/spec.md`
2. Найти FR-041 и FR-055
3. Удалить дублирование, оставить ссылку на FR-068:
   ```
   - **FR-041**: Система ДОЛЖНА использовать единую типографику (см. FR-068 для деталей)
   - **FR-055**: Система ДОЛЖНА применять типографику с line-height и max-width (см. FR-068 для деталей)
   ```
4. Убедиться что FR-068 содержит полные детали типографики (размеры, line-height, max-width)

**Файлы для изменения**:

- `specs/002-sifs-web-enhancement/spec.md` (FR-041, FR-055)

---

## Итоговая статистика плана

**Всего задач**: 50

- **Этап 1 (CRITICAL)**: 2 задачи (R1-R2)
- **Этап 2 (HIGH)**: 12 задач (R3-R14)
- **Этап 3 (MEDIUM)**: 25 задач (R15-R39)
- **Этап 4 (LOW)**: 11 задач (R40-R50)

**Оценка времени**:

- Этап 1: 1-2 часа
- Этап 2: 4-6 часов
- Этап 3: 8-12 часов
- Этап 4: 4-6 часов
- **Итого**: 17-26 часов

**Приоритет выполнения**:

1. 🔴 Этап 1 - немедленно (блокирует реализацию)
2. 🟠 Этап 2 - перед MVP (критично для качества)
3. 🟡 Этап 3 - перед production (улучшает качество)
4. 🟢 Этап 4 - по возможности (nice to have)

---

## Рекомендации по выполнению

1. **Выполнять последовательно по этапам**: не переходить к следующему этапу пока не завершён предыдущий
2. **Параллельное выполнение**: задачи в рамках одного этапа можно выполнять параллельно (если помечены [P])
3. **Проверка после каждого этапа**: после завершения этапа проверить что все задачи выполнены и критерии успеха достигнуты
4. **Обновление артефактов**: после выполнения задач обновить spec.md, plan.md и tasks.md соответственно

---

**План создан**: 2025-01-27  
**Статус**: ✅ Готов к выполнению
