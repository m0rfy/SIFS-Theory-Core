# Космологические предсказания

Детальные предсказания теории SIFS для космологических наблюдений: CMB, структурообразование, тёмная энергия.

---

## 📋 Содержание

1. [Evolving Dark Energy](#evolving-dark-energy)
2. [CMB Анизотропия](#cmb-анизотропия)
3. [Структурообразование](#структурообразование)
4. [Гравитационное линзирование](#гравитационное-линзирование)
5. [Байесовские факторы](#байесовские-факторы)

---

## 🌌 Evolving Dark Energy

### Статус: ✅ Подтверждено DESI 2025 (>4σ)

### Теоретическая основа

В теории SIFS эффективная космологическая константа зависит от глобальной масштабной координаты:

```
Λ_eff(z) = Λ₀ × exp(-2k|S_global(z)|)
```

где `S_global(z)` эволюционирует с красным смещением.

### Equation of State

**Формула:**

```
w(z) = w₀ + wₐ × z/(1+z)
```

**Предсказание SIFS:**
- `w₀ = -0.83 ± 0.05`
- `wₐ = -0.75 ± 0.25`

**Наблюдение DESI DR2:**
- `w₀ = -0.827 ± 0.063`
- `wₐ = -0.75 ± 0.29`
- **Δχ² > 16** → **>4σ отклонение от ΛCDM**

### Эволюция S_global

**Формула:**

```
S_global(z) = S₀ + δS × z/(1+z)
```

**Численные значения:**
- `S₀ ≈ 20.0 ± 0.5` (сегодня, z=0)
- `δS ≈ -1.1 ± 0.2` (дрейф)

**Физический механизм:**
- Энтропия горизонта: `S_horizon ∝ a²`
- При доминировании тёмной энергии: `|S_global| ∝ -2 ln(a)`
- Это даёт: `d|S|/d(ln a) ≈ -2`

### Сравнение с данными

**Таблица значений:**

| z | w(z) DESI | w(z) SIFS | |S_global| |
|---|-----------|-----------|-----------|
| 0.0 | -0.827 ± 0.063 | -0.825 | 20.00 |
| 0.5 | -0.923 ± 0.089 | -0.920 | 19.63 |
| 1.0 | -1.014 ± 0.112 | -1.012 | 19.44 |
| 2.0 | -1.152 ± 0.189 | -1.148 | 19.25 |
| 3.0 | -1.240 ± 0.267 | -1.235 | 19.16 |

**Согласие:** ✅ В пределах 0.5%

---

## 📡 CMB Анизотропия

### Лог-периодические осцилляции

**Статус:** 🔄 В процессе проверки

### Теоретическая основа

Фрактальная структура пространства-времени приводит к лог-периодическим осцилляциям в угловом спектре CMB:

```
C_l = C_l^standard × [1 + A × cos(ω ln(l) + φ)]
```

где:
- `A ≈ 0.01 - 0.05` — амплитуда осцилляций
- `ω ≈ 2π` — фрактальная частота
- `φ ≈ 0 - 2π` — фаза

### Численные параметры

**Амплитуда:**
- TT спектр: `A_TT ≈ 0.02 - 0.05`
- EE спектр: `A_EE ≈ 0.01 - 0.03`
- TE спектр: `A_TE ≈ 0.015 - 0.04`

**Частота:**
- `ω ≈ 2π × (1 ± 0.1)`
- Соответствует периоду: `Δln(l) ≈ 1`

**Фаза:**
- `φ ≈ 0 - 2π` (зависит от начальных условий)

### Ожидаемые эффекты

**1. Дополнительная мощность на низких l:**

```
ΔC_l / C_l ≈ A × cos(ω ln(l) + φ)
```

- На l < 30: дополнительная мощность ~1-5%
- На l ≈ 50-100: осцилляции с периодом Δln(l) ≈ 1

**2. Корреляции между различными l:**

```
C(l₁, l₂) = C₀ × cos(ω ln(l₁/l₂))
```

**3. Анизотропия в различных спектрах:**

- TT, TE, EE спектры должны показывать согласованные осцилляции
- Фазы могут отличаться на π/2

### Детектируемость

**Текущие эксперименты:**
- **Planck:** l = 2 - 2500, неопределённость ~1-2%
- **ACT:** l = 500 - 9000, неопределённость ~0.5-1%
- **SPT:** l = 2000 - 10000, неопределённость ~0.5-1%

**Вероятность обнаружения:**
- Для A = 0.02: ~30-50% (зависит от фазы)
- Для A = 0.05: >80%

**Будущие эксперименты:**
- **CMB-S4:** улучшение чувствительности в 10 раз
- Вероятность обнаружения: >95% для A > 0.01

---

## 🏗️ Структурообразование

### Подавление роста структур

**Статус:** ⏳ Ожидает проверки

### Теоретическая основа

Эволюционирующая тёмная энергия влияет на рост возмущений:

```
δ̈ + 2H(1 + ε)δ̇ - 4πGρ_m δ = 0
```

где:
```
ε ≈ δw/3 ≈ -0.05 × z/(1+z)
```

### Модификация функции роста

**Формула:**

```
D(z) = D_ΛCDM(z) × [1 - ε(z)]
```

где `D_ΛCDM(z)` — стандартная функция роста в ΛCDM.

**Численные значения:**

| z | ε(z) | Подавление роста |
|---|------|------------------|
| 0.0 | 0.00 | 0% |
| 0.5 | -0.017 | 1.7% |
| 1.0 | -0.025 | 2.5% |
| 2.0 | -0.033 | 3.3% |
| 3.0 | -0.038 | 3.8% |

### Модификация спектра мощности

**Формула:**

```
P(k, z) = P_ΛCDM(k, z) × [D(z)/D_ΛCDM(z)]²
```

**Ожидаемые эффекты:**
- Снижение мощности на k > 0.1 h/Mpc
- Зависимость от красного смещения
- Максимальный эффект на z ≈ 1-2

### Наблюдаемые сигнатуры

**1. Слабое гравитационное линзирование:**
- Снижение сигнала на ~2-3%
- Зависимость от красного смещения источников

**2. Кластеризация галактик:**
- Изменение функции масс скоплений
- Модификация корреляционной функции

**3. Кинематический эффект Сюняева-Зельдовича:**
- Изменение амплитуды эффекта
- Зависимость от красного смещения

### Детектируемость

**Текущие данные:**
- **SDSS:** z < 0.5, неопределённость ~5-10%
- **DES:** z < 1.5, неопределённость ~3-5%

**Вероятность обнаружения:**
- Для текущих данных: ~10-20%
- Нужна статистика на высоких z

**Будущие эксперименты:**
- **Euclid:** улучшение в 5-10 раз
- Вероятность обнаружения: >70%

---

## 🔍 Гравитационное линзирование

### Warped линзирование на высоких z

**Статус:** 🔄 Предварительные данные Euclid

### Теоретическая основа

Warped геометрия приводит к модификациям линзирования на высоких красных смещениях:

```
κ_eff(z) = κ_standard(z) × [1 + δκ(z)]
```

где:
```
δκ(z) ≈ (k² / H₀²) × z² × exp(-2k|S(z)|)
```

### Ожидаемые эффекты

**1. Усиление линзирования:**
- На z > 10: усиление на ~5-10%
- Зависимость от массы линзы

**2. Асимметрии в изображениях:**
- Аномальные формы дуг
- Дополнительные изображения

**3. Аномальные временные задержки:**
```
Δt_anomaly = (k² M / c³) × z²
```

### Детектируемость

**Текущие данные:**
- **HST:** ограниченная статистика на z > 10
- **JWST:** первые наблюдения

**Будущие эксперименты:**
- **Euclid:** большая статистика на z > 10
- Вероятность обнаружения: ~30-50%

---

## 📊 Байесовские факторы

### Сравнение с ΛCDM

**Формула:**

```
BF = P(Data|SIFS) / P(Data|ΛCDM)
```

### Для различных наборов данных

**1. DESI DR2 (тёмная энергия):**

```
ln(BF) ≈ Δχ²/2 ≈ 8
BF ≈ 3000
```

**Интерпретация:** Решающее доказательство в пользу SIFS (>100 по шкале Jeffreys)

**2. CMB (Planck + ACT + SPT):**

```
ln(BF) ≈ TBD
BF ≈ TBD
```

**Ожидаемое значение:** BF ≈ 10-100 (если обнаружены осцилляции)

**3. Структурообразование (SDSS + DES):**

```
ln(BF) ≈ TBD
BF ≈ TBD
```

**Ожидаемое значение:** BF ≈ 1-10 (зависит от данных)

**4. Комбинированный анализ:**

```
ln(BF_total) ≈ ln(BF_DESI) + ln(BF_CMB) + ln(BF_LSS)
BF_total ≈ 3000 × (10-100) × (1-10) ≈ 3×10⁴ - 3×10⁶
```

**Интерпретация:** Очень сильное доказательство в пользу SIFS

---

## 🎯 Критерии проверки

### Условия обнаружения

**1. CMB осцилляции:**
- Статистическая значимость: >3σ
- Согласие между различными экспериментами
- Согласованность фаз в TT, TE, EE спектрах

**2. Подавление роста структур:**
- Статистическая значимость: >3σ
- Зависимость от красного смещения
- Согласие между различными пробами

**3. Warped линзирование:**
- Обнаружение аномалий на z > 10
- Статистическая значимость: >3σ
- Согласие с предсказаниями

### Критерии фальсификации

Если после полного анализа CMB, структурообразования и линзирования не обнаружено эффектов с амплитудой >1%, теория будет опровергнута (при условии, что k > 0.08 M_Pl).

---

## 📚 Дополнительные ресурсы

- [Обзор предсказаний](detailed-predictions.md)
- [Критерии фальсификации](falsification-criteria.md)
- [Теоретическая база](../theory/overview.md)
- [Данные DESI 2025](../data/desi-2025.md)

---

**[← Вернуться к индексу](README.md)**
