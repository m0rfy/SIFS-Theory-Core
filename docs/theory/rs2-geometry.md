# RS2 Geometry: Randall-Sundrum Warped Spacetime

Геометрическая основа теории SIFS: warped 5D пространство Randall-Sundrum второго типа.

## 🌌 Введение

RS2 модель (Randall-Sundrum, 1999) решает **проблему иерархии** через экспоненциальное warping метрики в дополнительном измерении.

В отличие от RS1 (две браны), RS2 имеет:
- **Одну брану** в 5D bulk
- **Бесконечное** пятое измерение
- **AdS₅ геометрию** bulk-пространства

---

## 📐 5D Метрика

### Линейный элемент

```
ds² = exp(−2k|y|) η_μν dx^μ dx^ν + dy²
```

где:
- `y` — координата вдоль 5-го измерения
- `k` — warping параметр (AdS curvature scale)
- `η_μν = diag(1, −1, −1, −1)` — метрика Минковского на бране
- `x^μ = (t, x, y, z)` — координаты на бране

### SIFS модификация

В SIFS мы используем **масштабную координату** `S` вместо `y`:

```
ds² = exp(−2k|S|) η_μν dx^μ dx^ν + dS²
```

где `S` интерпретируется как **логарифмический масштаб**:

```
S = ln(λ/l_Pl)
```

---

## 🔧 Уравнения Эйнштейна в 5D

### Bulk action

```
S_bulk = M₅³ ∫ d⁵x √|g| (R₅ + 2Λ₅)
```

где:
- `M₅` — 5D планковская масса
- `R₅` — 5D скаляр Риччи
- `Λ₅ = −6k²` — отрицательная космологическая константа (AdS)

### Уравнения поля

```
R_AB − (1/2) g_AB R = (1/M₅³) (T_AB + Λ₅ g_AB)
```

### Решение для пустого bulk

Для `T_AB = 0`:

```
g_μν = exp(−2k|y|) η_μν
g_yy = 1
```

удовлетворяет уравнениям с `Λ₅ = −6k²`.

---

## 🎯 Warping Factor

### Экспоненциальное подавление

Ключевая особенность RS2 — **warp factor**:

```
A(y) = exp(−k|y|)
```

Физические величины на бране масштабируются:

```
m_physical = m_bare × A(y) = m_bare × exp(−k|y|)
```

### Иерархия масс

Если брана Планка находится в `y = 0`, а наша брана в `y = L`:

```
M_weak / M_Pl = exp(−kL)
```

Для `kL ≈ 35`:

```
M_weak / M_Pl ≈ exp(−35) ≈ 10⁻¹⁵
```

**Решает проблему иерархии!**

---

## 🌊 Bulk Dynamics

### Kaluza-Klein modes

Разложение по модам в 5D:

```
φ(x^μ, y) = Σₙ φₙ(x^μ) ψₙ(y)
```

Волновое уравнение для `ψₙ(y)`:

```
[−∂²_y + m²_KK(y)] ψₙ = mₙ² ψₙ
```

где эффективная масса:

```
m²_KK(y) = (k²/4) exp(−2ky)
```

### Zero mode

Легчайшая мода (`n = 0`):

```
ψ₀(y) ∝ exp(2ky)
```

**Локализована на бране** (экспоненциально убывает в bulk).

### Массивные моды

Массы KK-мод:

```
mₙ ≈ k × (2n + const)
```

Для `k ≈ M_Pl`:
```
m₁ ≈ M_Pl ≈ 10¹⁹ GeV
```

**Недостижимо для коллайдеров!**

---

## 🔬 Junction Conditions

### Israel equations

На границе bulk-brane:

```
[K_μν] = (1/M₅³) (T_μν − (1/3) T g_μν)
```

где `[K_μν]` — скачок внешней кривизны:

```
K_μν = (1/2) ∂_n g_μν
```

### Натяжение браны

Для RS2:

```
σ = 6k M₅³
```

Положительное натяжение стабилизирует решение.

---

## 📊 Физические следствия

### 1. Гравитация на бране

Эффективная 4D гравитация:

```
S_eff = M_Pl² ∫ d⁴x √|g₄| R₄
```

где:

```
M_Pl² = (M₅³/k)
```

### 2. Подавление массы

Частицы на бране в `y = L`:

```
m_obs = M_bare × exp(−kL)
```

Если `M_bare ≈ M_Pl` и `kL ≈ 35`:

```
m_obs ≈ 100 GeV
```

### 3. Сильное искривление в bulk

Скаляр Риччи:

```
R₅ = −20k²
```

Сильная кривизна в bulk, но **плоская метрика на бране**!

---

## 🎨 Визуализация

### 2D проекция

```
        y
        ↑
        |
   bulk |████████████ (AdS₅)
        |
 brane  |━━━━━━━━━━━━ (Minkowski)
        |
        └────────────→ x
```

Метрика:
- **На бране** (`y = 0`): плоская Минковского
- **В bulk** (`y > 0`): экспоненциально искривлена

### Warp factor профиль

```
A(y)
  1 |●
    |  ●
    |    ●
    |      ●
    |         ●
    |              ●
    |                      ●
  0 |_________________________●____
    0                            y
```

Экспоненциальное убывание: `A(y) = exp(−ky)`

---

## 🧮 Численные параметры SIFS

### Масштаб warping

```
k ≈ 0.1 M_Pl ≈ 2.2 × 10¹⁸ GeV
```

### Калибровка

Из наблюдаемой гравитационной константы:

```
G_obs = 6.674 × 10⁻¹¹ m³/(kg·s²)

M_Pl = (ħc/G)^(1/2) = 1.22 × 10¹⁹ GeV
```

### Типичные значения |S|

| Объект | Масштаб | \|S\| |
|--------|---------|-------|
| Планк | 10⁻³⁵ m | 0 |
| Протон | 10⁻¹⁵ m | 11.2 |
| Атом | 10⁻¹⁰ m | 16 |
| Солнце | 10⁹ m | 23.5 |
| Хаббл | 10²⁶ m | 28 |

---

## ⚠️ Проблемы RS2

### 1. Стабильность радиона

Расстояние между бранами (radion) должно быть стабилизировано.

**Решения:**
- Goldberger-Wise механизм (bulk scalar)
- Casimir энергия
- В SIFS: фрактальная структура стабилизирует `|S|`

### 2. Cosmological constant

Fine-tuning космологической константы.

**SIFS решение:**
Evolving `Λ_eff(S_global)` согласуется с DESI 2025.

### 3. Hierarchy problem

RS2 решает только если `kL` fine-tuned.

**SIFS решение:**
Резонансные моды `|S|` определяют иерархию без tuning.

---

## 🔗 Связь с AdS/CFT

RS2 — гравитационная реализация **AdS/CFT correspondence**:

```
Gravity in AdS₅ ↔ CFT₄ on boundary
```

В SIFS:
- **Bulk** = фрактальное 5D пространство
- **Brane** = наша 4D вселенная
- **Голография** = проекция bulk-физики на брану

---

## 📚 References

1. **Randall, L., Sundrum, R.** (1999). "An Alternative to Compactification." *Phys. Rev. Lett.* **83**, 4690. [arXiv:hep-th/9906064](https://arxiv.org/abs/hep-th/9906064)

2. **Goldberger, W.D., Wise, M.B.** (1999). "Modulus Stabilization with Bulk Fields." *Phys. Rev. Lett.* **83**, 4922. [arXiv:hep-ph/9907447](https://arxiv.org/abs/hep-ph/9907447)

3. **Csaki, C., et al.** (2000). "Cosmology of the Randall-Sundrum Model." *Phys. Rev. D* **62**, 045015. [arXiv:hep-ph/9912208](https://arxiv.org/abs/hep-ph/9912208)

---

**[← Вернуться к теории](../README.md)**
