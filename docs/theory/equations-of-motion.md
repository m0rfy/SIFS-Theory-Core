# Уравнения движения

Полные уравнения движения теории SIFS с решениями, начальными условиями и асимптотиками.

---

## 📋 Содержание

1. [Уравнения в bulk](#уравнения-в-bulk)
2. [Уравнения на бране](#уравнения-на-бране)
3. [Решения с начальными условиями](#решения-с-начальными-условиями)
4. [Асимптотики](#асимптотики)
5. [Связь с предсказаниями](#связь-с-предсказаниями)

---

## 🌌 Уравнения в bulk

### Действие в 5D

Полное действие теории:

```
S = S_bulk + S_brane

S_bulk = ∫ d⁴x dS √|g| [M₅³ R₅ + L_matter]

S_brane = ∫ d⁴x √|g₄| [-σ + L_brane] δ(S - S₀)
```

где:
- `M₅` — 5D планковская масса
- `R₅` — скаляр Риччи в 5D
- `σ` — натяжение браны
- `L_matter` — материя в bulk
- `L_brane` — материя на бране

### Уравнения Эйнштейна в bulk

**Полные уравнения:**

```
G_AB = (1/M₅³) T_AB
```

где:
- `G_AB = R_AB - (1/2) g_AB R` — тензор Эйнштейна в 5D
- `T_AB` — тензор энергии-импульса в 5D

**Компоненты:**

```
G_μν = (1/M₅³) T_μν
G_μS = (1/M₅³) T_μS
G_SS = (1/M₅³) T_SS
```

### Решение для warped метрики

**Метрика:**

```
ds² = exp(-2k|S|) η_μν dx^μ dx^ν + dS²
```

**Условия решения:**

1. **AdS₅ bulk:**
   ```
   Λ₅ = -6k²
   ```

2. **Натяжение браны:**
   ```
   σ = 6k M₅³
   ```

3. **Уравнения Эйнштейна:**
   ```
   R_AB - (1/2) g_AB R = -Λ₅ g_AB
   ```

---

## 🌐 Уравнения на бране

### Модифицированные уравнения Эйнштейна

**На бране (S = S₀):**

```
G_μν = κ₄² T_μν + κ₅⁴ S_μν - E_μν
```

где:
- `κ₄² = 8πG_4 = 1/M_Pl²` — 4D гравитационная константа
- `T_μν` — тензор энергии-импульса на бране
- `S_μν` — квадратичные по энергии-импульсу члены
- `E_μν` — проекция 5D тензора Вейля (dark radiation)

### Квадратичные члены

**Формула:**

```
S_μν = (1/12) [T T_μν - T_μα T^α_ν] + (1/24) g_μν [3T_αβ T^αβ - T²]
```

где `T = T^μ_μ` — след тензора энергии-импульса.

**Физический смысл:**
- Важны при высоких энергиях
- Модифицируют гравитацию на малых масштабах

### Dark radiation

**Проекция тензора Вейля:**

```
E_μν = C_μSνS |_{S=S₀}
```

**Свойства:**
- Безследовый: `E^μ_μ = 0`
- Бездивергентный: `∇^μ E_μν = 0`
- Зависит от состояния bulk

---

## 🔄 Решения с начальными условиями

### Космологическое решение

**Метрика Фридмана-Робертсона-Уокера на бране:**

```
ds² = -dt² + a²(t) [dr²/(1-kr²) + r²(dθ² + sin²θ dφ²)]
```

**Уравнения Фридмана:**

```
H² = (8πG/3) ρ + (κ₅⁴/36) ρ² - k/a² + Λ/3 + E/a⁴
```

где:
- `H = ȧ/a` — параметр Хаббла
- `ρ` — плотность энергии
- `k` — кривизна пространства
- `Λ` — космологическая константа
- `E/a⁴` — dark radiation

### Начальные условия

**При t = t₀:**

```
a(t₀) = a₀
ȧ(t₀) = H₀ a₀
ρ(t₀) = ρ₀
```

**Эволюция:**

```
a(t) = a₀ × exp[∫ H(t') dt']
```

### Решение для тёмной энергии

**Эффективная космологическая константа:**

```
Λ_eff(t) = Λ₀ × exp(-2k|S_global(t)|)
```

**Эволюция S_global:**

```
S_global(t) = S₀ + δS × f(t)
```

где `f(t)` — функция времени, определяемая энтропией горизонта.

---

## 📈 Асимптотики

### Ранняя Вселенная (t → 0)

**Поведение:**

```
a(t) ∝ t^(1/2)  (радиационное доминирование)
a(t) ∝ t^(2/3)  (материальное доминирование)
```

**Масштабная координата:**

```
|S_global| → ∞  (при t → 0)
```

**Эффективная космологическая константа:**

```
Λ_eff → 0  (при t → 0)
```

### Поздняя Вселенная (t → ∞)

**Поведение:**

```
a(t) ∝ exp(H₀ t)  (доминирование тёмной энергии)
```

**Масштабная координата:**

```
|S_global| → S₀  (стабилизация)
```

**Эффективная космологическая константа:**

```
Λ_eff → Λ₀ × exp(-2k S₀)  (константа)
```

### Малые масштабы (|S| → 0)

**Поведение метрики:**

```
ds² → η_μν dx^μ dx^ν + dS²  (плоская метрика)
```

**Эффективная гравитационная константа:**

```
G_eff → G_Pl  (планковская гравитация)
```

### Большие масштабы (|S| → ∞)

**Поведение метрики:**

```
ds² → exp(-2k|S|) η_μν dx^μ dx^ν + dS²  (сильное подавление)
```

**Эффективная гравитационная константа:**

```
G_eff → G_Pl × exp(-2k|S|) → 0  (слабая гравитация)
```

---

## 🔗 Связь с предсказаниями

### Evolving Dark Energy

**Уравнение состояния:**

```
w(z) = -1 + (2k/3) × d|S_global|/d(ln a)
```

**Связь с наблюдениями:**

```
w₀ = -1 + (2k/3) × d|S_global|/d(ln a)|_{z=0}
wₐ = (2k/3) × d²|S_global|/d(ln a)²
```

### Структурообразование

**Уравнение роста возмущений:**

```
δ̈ + 2H(1 + ε)δ̇ - 4πG_eff ρ_m δ = 0
```

где:
```
ε = (k/3) × d|S_global|/d(ln a)
```

**Модификация функции роста:**

```
D(z) = D_ΛCDM(z) × [1 - ε(z)]
```

### Гравитационные волны

**Модификация фазы:**

```
δφ = (k² M² / d) × (1+z) × cos(ω_GW t)
```

**Связь с уравнениями движения:**

Модификации возникают из-за warped геометрии в уравнениях Эйнштейна.

---

## 📚 Дополнительные ресурсы

- [Математический формализм](mathematics.md)
- [Теоретический обзор](overview.md)
- [Предсказания](../predictions/detailed-predictions.md)

---

**[← Вернуться к документации](../README.md)**
