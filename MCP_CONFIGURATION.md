# Конфигурация MCP для Cursor IDE

## ✅ Настройка завершена

Конфигурация MCP для генерации изображений с OpenAI настроена и готова к использованию.

## 📁 Расположение файлов

### Основная конфигурация MCP
- **Файл**: `.cursor/mcp.json` ✅ (правильное расположение согласно документации Cursor)
- **Резервная копия**: `.cursor/rules/mcp.json` (можно удалить)

### Конфигурация shadcn
- **Файл**: `.cursor/shadcn-config.json`
- **Дубликат**: `.cursor/shadcn.json` (можно удалить)

## 🔧 Настроенные MCP серверы

### 1. `openai-image-generation` (основной)
- **Команда**: `uvx`
- **Пакет**: `mcp-openai-image-generation@latest`
- **Статус**: ✅ Включен
- **API ключ**: Настроен

### 2. `dalle-mcp` (резервный)
- **Команда**: `node`
- **Путь**: `C:\SIFS-Theory-Core\dalle-mcp\build\index.js`
- **Статус**: ✅ Включен
- **API ключ**: Настроен
- **Директория сохранения**: `C:\SIFS-Theory-Core`

## 🚀 Использование

1. **Перезапустите Cursor IDE** для применения конфигурации
2. После перезапуска MCP серверы будут доступны через чат
3. Используйте команды для генерации изображений:
   - `generate_image` - генерация изображений
   - `edit_image` - редактирование изображений
   - `create_variation` - создание вариаций
   - `validate_key` - проверка API ключа

## 🔐 Безопасность

- ✅ Файл `.cursor/mcp.json` добавлен в `.gitignore`
- ✅ API ключ не будет закоммичен в репозиторий
- ✅ Конфигурация доступна только локально

## 📝 Примечания

- При первом использовании `uvx` может загрузить пакет `mcp-openai-image-generation`
- Если возникнут проблемы с `uvx`, используйте локальный сервер `dalle-mcp`
- Сгенерированные изображения сохраняются в `C:\SIFS-Theory-Core`

## 🔄 Обновление конфигурации

Для обновления конфигурации отредактируйте файл `.cursor/mcp.json` и перезапустите Cursor IDE.
