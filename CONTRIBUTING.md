# Contributing to SIFS Theory

Спасибо за интерес к проекту SIFS Theory! Мы приветствуем вклад в развитие теории, документации и визуализаций.

## 🎯 Как внести вклад

### 1. Сообщить об ошибке

Если вы нашли ошибку:
- Проверьте, нет ли уже [существующей issue](https://github.com/your-username/sifs-theory/issues)
- Создайте новую issue с подробным описанием
- Укажите шаги для воспроизведения
- Приложите скриншоты, если применимо

### 2. Предложить улучшение

Для новых фич или улучшений:
- Откройте issue с описанием предложения
- Объясните, какую проблему это решает
- Дождитесь обсуждения перед началом работы

### 3. Создать Pull Request

1. **Fork репозитория**
   ```bash
   git clone https://github.com/your-username/sifs-theory.git
   cd sifs-theory
   ```

2. **Создайте ветку для изменений**
   ```bash
   git checkout -b feature/your-feature-name
   ```

3. **Внесите изменения**
   - Следуйте стилю кода проекта
   - Добавьте тесты, если применимо
   - Обновите документацию

4. **Commit изменений**
   ```bash
   git add .
   git commit -m "feat: добавлена новая функция"
   ```

5. **Push и создание PR**
   ```bash
   git push origin feature/your-feature-name
   ```
   Затем откройте Pull Request на GitHub

## 📋 Правила для кода

### TypeScript/React

- Используйте TypeScript для типизации
- Компоненты должны быть функциональными с hooks
- Следуйте принципу единственной ответственности
- Добавляйте комментарии для сложной логики

### Стиль кода

```typescript
// ✅ Хорошо
interface SlideProps {
  title: string;
  subtitle?: string;
  slideNumber: number;
}

export function MySlide({ title, subtitle, slideNumber }: SlideProps) {
  return (
    <div className="p-4">
      <h1>{title}</h1>
      {subtitle && <p>{subtitle}</p>}
    </div>
  );
}

// ❌ Плохо
function myslide(props: any) {
  return <div><h1>{props.title}</h1></div>
}
```

### Commit messages

Следуйте [Conventional Commits](https://www.conventionalcommits.org/):

- `feat:` - новая функциональность
- `fix:` - исправление ошибки
- `docs:` - изменения документации
- `style:` - форматирование кода
- `refactor:` - рефакторинг
- `test:` - добавление тестов
- `chore:` - обновление зависимостей, конфигурации

Примеры:
```
feat: добавлен график эволюции тёмной энергии
fix: исправлена ошибка в расчёте RS-warping
docs: обновлена документация по константам связи
```

## 🧮 Научные вклады

### Математические расчёты

Если вы добавляете новые расчёты:
- Используйте реальные физические константы
- Укажите источники данных
- Добавьте единицы измерения
- Проверьте размерности

### Диаграммы и визуализации

Для новых визуализаций:
- Используйте recharts для графиков
- Следуйте цветовой схеме проекта
- Добавьте легенду и подписи осей
- Обеспечьте responsiveness

### Ссылки на литературу

Формат ссылок:
```markdown
**Автор(ы)** (Год). "Название статьи." *Журнал* Том, Страницы. [arXiv:XXXX.XXXXX](https://arxiv.org/abs/XXXX.XXXXX)
```

## 🎨 Визуальный стиль

### Цветовая палитра

```css
/* Основные цвета */
--cyan-400: #22d3ee     /* Математика, формулы */
--purple-400: #c084fc   /* Геометрия, RS2 */
--blue-400: #60a5fa     /* Физические данные */
--green-400: #4ade80    /* Подтверждения */
--orange-400: #fb923c   /* Оптическая метрика */
```

### Компоненты

- Используйте существующие UI компоненты из `/src/app/components/ui/`
- Для карточек: `bg-black/60 backdrop-blur-sm border border-white/10`
- Для hover эффектов: `hover:border-white/30 transition-all`

## 📚 Документация

При добавлении документации:
- Используйте Markdown
- Включайте примеры кода
- Добавляйте математические формулы в LaTeX
- Структурируйте с заголовками

## ✅ Checklist перед PR

- [ ] Код компилируется без ошибок (`npm run build`)
- [ ] Следует стилю проекта
- [ ] Добавлены комментарии для сложных частей
- [ ] Обновлена документация (если нужно)
- [ ] Commit messages следуют Conventional Commits
- [ ] PR описывает, что и зачем изменено

## 🤝 Код поведения

- Будьте уважительны к другим участникам
- Конструктивная критика приветствуется
- Научные дискуссии должны основываться на фактах
- Не допускается дискриминация любого вида

## 📬 Вопросы?

Если у вас есть вопросы:
- Откройте [Discussion](https://github.com/your-username/sifs-theory/discussions)
- Напишите в issue с тегом `question`
- Свяжитесь с мейнтейнерами

---

**Спасибо за вклад в SIFS Theory! 🌌**
