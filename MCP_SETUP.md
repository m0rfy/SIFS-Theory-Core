# Настройка MCP для генерации изображений с OpenAI

## ✅ Установка завершена

MCP серверы для генерации изображений настроены и готовы к использованию.

## 📋 Конфигурация

### 1. MCP серверы

В файле `.cursor/rules/mcp.json` настроены два сервера:

#### `openai-image-generation` (основной)
- Использует пакет `mcp-openai-image-generation@latest` через `uvx`
- API ключ OpenAI настроен
- Готов к использованию сразу после перезапуска Cursor IDE

#### `dalle-mcp` (резервный)
- Локальный сервер в директории `dalle-mcp/`
- Также настроен с вашим API ключом
- Сохраняет изображения в корневую директорию проекта

### 2. Конфигурация shadcn

Конфигурация shadcn сохранена в файле `.cursor/shadcn.json` со всеми указанными реестрами.

## 🚀 Использование

1. **Перезапустите Cursor IDE** для применения изменений конфигурации MCP

2. После перезапуска вы сможете использовать MCP инструменты для генерации изображений:
   - Запросите генерацию изображения через чат
   - MCP сервер автоматически использует ваш API ключ OpenAI

## 🔑 API ключ

Ваш API ключ OpenAI уже настроен в конфигурации:
- Ключ сохранен в `.cursor/rules/mcp.json`
- **Важно**: Не коммитьте этот файл в публичный репозиторий!

## 📝 Дополнительные файлы

- `mcp-openai-server.py` - Python скрипт для альтернативной реализации (опционально)
- `requirements-mcp.txt` - Зависимости для Python скрипта (если понадобится)
- `dalle-mcp/` - Локальный MCP сервер (уже собран и готов к использованию)

## ⚠️ Важные замечания

1. Убедитесь, что `uvx` установлен (проверено: ✅ установлен)
2. При первом использовании `uvx` может загрузить пакет `mcp-openai-image-generation`
3. Если возникнут проблемы с `uvx`, можно использовать локальный сервер `dalle-mcp`

## 🔧 Устранение неполадок

Если MCP сервер не работает:
1. Проверьте, что Cursor IDE перезапущен
2. Проверьте логи Cursor IDE на наличие ошибок
3. Убедитесь, что API ключ OpenAI действителен
4. Попробуйте использовать локальный сервер `dalle-mcp` вместо `openai-image-generation`
